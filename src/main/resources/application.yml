server:
  port: 8083
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/products
  cloud:
    config:
      import-check:
        enabled: false
    enabled: false
  import:
    optional:  configserver:instead

#spring:
#  application:
#    name: TARJETA
#  config:
#    import: optional:configserver:http://localhost:8093/